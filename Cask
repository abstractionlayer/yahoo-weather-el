(package "yahoo-weather" "0.0.3" "Fetch Yahoo Weather forecast in your emacs agenda")

(files "yahoo-weather.el" "org-yahoo-weather.el")

;; Tell Cask to add package information to this file (so that we can have a
;; single file package).
(package-file "yahoo-weather.el")

(source melpa-stable)